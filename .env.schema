# @currentEnv=$APP_ENV
# @plugin(@varlock/keepass-plugin)
# @initKeePass(dbPath=$KP_DB_PATH, password=$KP_PASSWORD, useCli=forEnv(dev))

# ---

# @type=enum(dev, prod)
APP_ENV=dev

# @sensitive
KP_DB_PATH=

# @type=kdbxPassword @sensitive
KP_PASSWORD=

# @sensitive=false
ALLOWED_DEV_ORIGINS=kp("mirzaianov-com/ALLOWED_DEV_ORIGINS")
