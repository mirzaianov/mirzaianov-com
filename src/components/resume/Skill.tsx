const Skill = ({ skillName }) => {
  return (
    <p className="inline-block rounded-sm px-2 py-1 outline outline-1 -outline-offset-1 outline-primary">
      {skillName}
    </p>
  );
};

export default Skill;
